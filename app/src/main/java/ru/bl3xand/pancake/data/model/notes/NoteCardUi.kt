package ru.bl3xand.pancake.data.model.notes

/** Режим отображения мета-информации в карточке заметки. */
enum class NoteMetaMode {
    UPDATED,
    CREATED
}

object NoteColors {
    const val DEFAULT_MARKER = "default"
}